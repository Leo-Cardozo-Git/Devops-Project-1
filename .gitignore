# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# valores reais de ambiente — nunca commitar
*.tfvars
!*.tfvars.example

# credenciais AWS, caso alguem gere localmente por engano
.aws/
*.pem
*.pfx

# dependencias e artefatos da aplicacao
node_modules/
npm-debug.log

# SO / editor
.DS_Store
.vscode/